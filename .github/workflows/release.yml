name: Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: Release version from package.json, without the leading v
        required: true
        type: string

permissions:
  contents: read

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  test:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          persist-credentials: false

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.2.22

      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: 24

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Run checks
        run: bun run check

  build:
    needs: test
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    environment: production
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0
          fetch-tags: true

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.2.22

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Validate requested version matches package version
        run: |
          PKG_VERSION="$(node -p "require('./package.json').version")"
          INPUT_VERSION="${{ inputs.version }}"
          if [ "$INPUT_VERSION" != "$PKG_VERSION" ]; then
            echo "Requested version ($INPUT_VERSION) does not match package.json version ($PKG_VERSION)"
            exit 1
          fi

      - name: Build standalone binaries
        run: bun run build:cli

      - name: Generate checksums
        run: |
          cd dist
          shasum -a 256 codex-multitask-* > SHA256SUMS

      - name: Smoke test linux-x64 binary
        run: |
          chmod +x dist/codex-multitask-linux-x64
          ./dist/codex-multitask-linux-x64 --help

      - name: Create and push tag
        run: |
          TAG="v${{ inputs.version }}"
          if git rev-parse "$TAG" >/dev/null 2>&1; then
            echo "Tag $TAG already exists locally"
          else
            git tag "$TAG"
          fi
          git push origin "$TAG"

      - name: Publish release assets
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ inputs.version }}
          name: v${{ inputs.version }}
          generate_release_notes: true
          files: |
            dist/codex-multitask-darwin-arm64
            dist/codex-multitask-darwin-x64
            dist/codex-multitask-linux-arm64
            dist/codex-multitask-linux-x64
            dist/codex-multitask-windows-x64.exe
            dist/SHA256SUMS
